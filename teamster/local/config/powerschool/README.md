`query-*.yaml`
```yaml
ops:
  compose_queries:
    config:
      year_id: int
      tables:
      - name: str
        queries:
          - projection: str
          - q:
              selector: str
              value: any
```
